import { AbilityModifierPipe } from './ability-modifier.pipe';

describe('AbilityModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new AbilityModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
