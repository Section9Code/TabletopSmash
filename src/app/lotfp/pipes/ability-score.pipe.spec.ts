import { AbilityScorePipe } from './ability-score.pipe';

describe('AbilityScorePipe', () => {
  it('create an instance', () => {
    const pipe = new AbilityScorePipe();
    expect(pipe).toBeTruthy();
  });
});
